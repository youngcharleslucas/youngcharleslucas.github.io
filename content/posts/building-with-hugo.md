+++
title = "building a site with Hugo and GitHub Pages"
date = 2026-05-20
draft = true
tags = ["hugo", "web"]
+++

Hugo is fast and stays out of your way. A `hugo server -D` and you have a live preview. Push to `main` and GitHub Actions deploys it.

No Node, no bundler, no CMS. Just markdown files and a couple of layout templates.

The whole thing costs nothing to run.
